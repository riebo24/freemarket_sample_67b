- customer = Payjp::Customer.retrieve(card.customer_id)
- user_card = customer.cards.retrieve(card.card_id)

%br
= "**** **** **** " + user_card.last4
%br
- exp_month = user_card.exp_month.to_s
- exp_year = user_card.exp_year.to_s.slice(2,3)
= exp_month + " / " + exp_year
= form_tag(delete_user_cards_path, method: :post, id: 'charge-form',  name: "inputForm") do
  %input{ type: "hidden", name: "card_id", value: "" }
  %button 削除する


